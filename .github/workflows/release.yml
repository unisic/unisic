name: Release Packages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

defaults:
  run:
    shell: bash

env:
  BUILD_TYPE: Release
  # Baked into the binary by CMake and shown in the sidebar footer.
  UNISIC_BUILD_NUMBER: ${{ github.run_number }}

jobs:
  check:
    name: Version check
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.v.outputs.version }}
      should_release: ${{ steps.v.outputs.should_release }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
      - id: v
        name: Read version and decide whether to release
        run: |
          # Display version (allows pre-release suffixes like "0.3b"); falls
          # back to the numeric project() version if the string is absent.
          version_line="$(grep -m1 '^set(UNISIC_VERSION_STRING' CMakeLists.txt || true)"
          if [[ -n "$version_line" ]]; then
            version="$(sed -E 's/.*"([^"]+)".*/\1/' <<<"$version_line")"
          else
            version_line="$(grep -m1 '^project(unisic VERSION' CMakeLists.txt)"
            version="${version_line#*VERSION }"
            version="${version%% *}"
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          # Release only when this version has not been tagged yet — i.e. the
          # version in CMakeLists.txt was bumped. Plain pushes to main (docs,
          # refactors without a bump) build nothing and cut no release.
          if git rev-parse -q --verify "refs/tags/v${version}" >/dev/null; then
            echo "should_release=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Tag v${version} already exists — no version bump, skipping release."
          else
            echo "should_release=true" >> "$GITHUB_OUTPUT"
            echo "::notice::Releasing v${version}."
          fi

  # deb, rpm and arch are extracted to reusable workflows so build.yml's PR
  # smoke check runs the identical build (no drift). Each uploads the same
  # artifact name the release job downloads below.
  deb:
    name: Debian package
    needs: check
    if: ${{ needs.check.outputs.should_release == 'true' }}
    uses: ./.github/workflows/deb.yml

  rpm:
    name: RPM (Fedora)
    needs: check
    if: ${{ needs.check.outputs.should_release == 'true' }}
    uses: ./.github/workflows/rpm.yml

  arch:
    name: Arch package
    needs: check
    if: ${{ needs.check.outputs.should_release == 'true' }}
    uses: ./.github/workflows/arch.yml

  appimage:
    name: AppImage
    needs: check
    if: ${{ needs.check.outputs.should_release == 'true' }}
    # Extracted to a reusable workflow so build.yml's PR smoke check runs the
    # identical build (no drift). Uploads the unisic-appimage artifact the
    # release job downloads below.
    uses: ./.github/workflows/appimage.yml

  flatpak:
    name: Flatpak bundle
    needs: check
    if: ${{ needs.check.outputs.should_release == 'true' }}
    # Deliberately NOT in the release job's `needs`: the KDE runtime plus an
    # ffmpeg built from source makes this the slowest job by far, and a Flatpak
    # that fails to build must not hold back the packages that did. The bundle
    # is attached afterwards by flatpak-asset.
    #
    # local: true builds this checkout instead of the tag the manifest pins -
    # v<version> does not exist until the release job below creates it.
    uses: ./.github/workflows/flatpak.yml
    with:
      local: true

  release:
    name: GitHub Release
    needs:
      - check
      - deb
      - rpm
      - arch
      - appimage
    if: ${{ needs.check.outputs.should_release == 'true' }}
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.check.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history + tags so the changelog can diff against the previous
          # release and find the last clean semver tag.
          fetch-depth: 0
          submodules: recursive

      # Every package is attached to the GitHub release. The repos stay the
      # UPDATE channel — each direct-download package self-registers its native
      # repo on first install, so a one-off download keeps updating natively:
      #   .deb      → OBS apt repo   (packaging/deb/postinst)
      #   .rpm      → Fedora-only asset (.fedora — see the rpm job comment);
      #               %post registers the COPR dnf repo. openSUSE has no
      #               release rpm: it installs from the OBS repos (README)
      #   .pkg.tar.zst → OBS pacman repo (packaging/arch/unisic.install)
      #   AppImage  → self-updates via the embedded zsync info
      #   .flatpak  → attached by the flatpak-asset job, which runs after this
      #               one; install.sh installs from it until the Flathub
      #               listing is live, then moves the install to that remote
      - name: Download release assets
        uses: actions/download-artifact@v4
        with:
          # The pattern is what keeps the Flatpak bundle out: that job may well
          # still be running, and whether it happened to finish first must not
          # decide which assets a release carries.
          pattern: unisic-*
          path: dist
          merge-multiple: true

      - name: Generate changelog
        run: bash scripts/gen-changelog.sh "$VERSION" > NOTES.md

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          shopt -s globstar nullglob
          assets=()
          for path in dist/**/*; do
            [[ -f "$path" ]] && assets+=("$path")
          done
          if [[ ${#assets[@]} -eq 0 ]]; then
            printf 'No release assets found\n' >&2
            exit 1
          fi

          # A letter suffix in the version ("0.3b", "1.0rc1") marks the
          # GitHub release as a pre-release automatically.
          prerelease=()
          if [[ "$VERSION" =~ [a-zA-Z] ]]; then
            prerelease=(--prerelease)
          fi
          gh release create "v${VERSION}" "${assets[@]}" \
            --target "$GITHUB_SHA" \
            --title "Unisic ${VERSION}" \
            --notes-file NOTES.md "${prerelease[@]}"

  flatpak-asset:
    name: Attach the Flatpak bundle
    # Both: `release` for the release to attach to, `flatpak` for the bundle.
    needs:
      - check
      - release
      - flatpak
    if: ${{ needs.check.outputs.should_release == 'true' }}
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.check.outputs.version }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: flatpak-bundle
          path: dist

      - name: Upload it to the release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: gh release upload "v${VERSION}" dist/*.flatpak --clobber

  obs:
    name: Trigger OBS rebuild
    # needs `release` so the v<version> tag exists when OBS's obs_scm service
    # fetches the repo; `check` only for the should_release output. The OBS
    # project (home:unisic) rebuilds Debian/Ubuntu/Arch/openSUSE
    # repos from the tag — see packaging/obs/README.md.
    needs:
      - check
      - release
    if: ${{ needs.check.outputs.should_release == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Run OBS source services (home:unisic/unisic)
        env:
          OBS_TOKEN: ${{ secrets.OBS_TOKEN }}
        run: |
          if [[ -z "$OBS_TOKEN" ]]; then
            echo "::error::OBS_TOKEN secret is not configured (see packaging/obs/README.md)"
            exit 1
          fi
          resp="$(curl -sS --fail-with-body -X POST \
            -H "Authorization: Token ${OBS_TOKEN}" \
            "https://api.opensuse.org/trigger/runservice?project=home:unisic&package=unisic")"
          echo "$resp"
          grep -q 'code="ok"' <<<"$resp" \
            || { echo "::error::OBS trigger did not return ok"; exit 1; }

  aur:
    name: AUR
    # After `release`, because sync.sh downloads the published assets (the
    # source tarball and the .pkg.tar.zst it repacks) to hash them - they do
    # not exist until the release job has created the release.
    needs:
      - check
      - release
    if: ${{ needs.check.outputs.should_release == 'true' }}
    runs-on: ubuntu-latest
    container: archlinux:base-devel
    env:
      VERSION: ${{ needs.check.outputs.version }}
    steps:
      - name: Install tooling
        run: |
          pacman -Sy --noconfirm archlinux-keyring
          pacman -Syu --noconfirm --needed git openssh namcap pacman-contrib

      - uses: actions/checkout@v4

      # makepkg refuses to run as root, and the container is root. Everything
      # below therefore runs as `builder`, which needs passwordless sudo for
      # makepkg -s to install build dependencies.
      - name: Create the build user
        run: |
          useradd -m builder
          printf 'builder ALL=(ALL) NOPASSWD: ALL\n' > /etc/sudoers.d/builder
          chown -R builder:builder "$GITHUB_WORKSPACE"

      - name: Configure SSH for the AUR
        env:
          AUR_SSH_KEY: ${{ secrets.AUR_SSH_KEY }}
        run: |
          if [[ -z "$AUR_SSH_KEY" ]]; then
            echo "::error::AUR_SSH_KEY secret is not configured (see packaging/aur/README.md)"
            exit 1
          fi
          install -d -m 700 -o builder -g builder /home/builder/.ssh
          printf '%s\n' "$AUR_SSH_KEY" > /home/builder/.ssh/aur
          # The host key is PINNED rather than ssh-keyscan'd: a keyscan trusts
          # whatever answers on the day, which is exactly the thing a known
          # hosts file exists to prevent.
          cat > /home/builder/.ssh/known_hosts <<'EOF'
          aur.archlinux.org ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEuBKrPzbawxA/k2g6NcyV5jmqwJ2s+zpgZGZ7tpLIcN
          EOF
          cat > /home/builder/.ssh/config <<'EOF'
          Host aur.archlinux.org
              User aur
              IdentityFile ~/.ssh/aur
              IdentitiesOnly yes
          EOF
          chown -R builder:builder /home/builder/.ssh
          chmod 600 /home/builder/.ssh/aur /home/builder/.ssh/config
          chmod 644 /home/builder/.ssh/known_hosts

      - name: Publish to the AUR
        run: |
          # Pre-releases keep out of the AUR: an AUR package with no explicit
          # channel is understood to be the stable one, and a helper would push
          # a beta onto every user who typed `paru -Syu`. The release job marks
          # these the same way - a letter anywhere in the version.
          if [[ "$VERSION" =~ [a-zA-Z] ]]; then
            echo "::notice::${VERSION} is a pre-release - leaving the AUR on the last stable version"
            exit 0
          fi
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          runuser -u builder -- bash -lc "
            set -e
            git config --global user.name 'Unisic release bot'
            git config --global user.email 'noreply@github.com'
            cd '$GITHUB_WORKSPACE/packaging/aur'
            ./sync.sh --version '$VERSION' --check bin --push
          "
